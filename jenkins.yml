version: "3.9"

services:

  identidock:
    build: .
    ports:
      - "9090:9090"
    environment:
      ENV: PROD
    depends_on:
      - dnmonster
      - redis

  dnmonster:
    image: amouat/dnmonster:1.0

  redis:
    image: redis:7-alpine
